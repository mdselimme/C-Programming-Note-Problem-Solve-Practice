#include <stdio.h>


int main(){
    printf("Assignment Operator.\n");
    //Assignment operator (=) that we called equal in math. It used in a variable to assign a value.
    int x = 0;
    // assignment operator (=) to assign the value 0 to a variable called x
    printf("Assignment : %d", x);
    return 0;
}