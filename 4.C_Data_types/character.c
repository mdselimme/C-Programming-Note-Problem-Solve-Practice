#include <stdio.h>

int main(){
    //character data types can not accept more than one character and it's format specifires is %c. 
    printf("Character data types\n");
    char s = 50, y = 60, z = 30;//these variables print ASCII symbol
    printf("character data types: s = %c, y = %c, z = %c;\n", s, y, z);
    return 0;
}