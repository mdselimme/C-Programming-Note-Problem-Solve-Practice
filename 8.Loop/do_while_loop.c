#include <stdio.h>

int main () {
    // initial value 
    int w = 0;
    // work in do loop
      do
    {
        printf("%d\n", w);
        //counting
        w++;
    } while (w < 10);//condtion write in while
    return 0;
}